rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura/escritura a todas las colecciones
    // (la autenticación la maneja la app con usuario/contraseña en Firestore)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
